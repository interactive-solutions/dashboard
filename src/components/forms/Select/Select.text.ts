import { defineMessages } from 'react-intl';

export const texts = defineMessages({
  emptyMenuItems: {
    id: 'Select.emptyMenuItems',
    description: 'Empty menu items',
    defaultMessage: 'Hittade inga resultat',
  },
});
