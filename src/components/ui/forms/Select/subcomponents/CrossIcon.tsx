import { ComponentProps } from 'react';

import { components } from 'react-select';

export const CrossIcon = (
  props: ComponentProps<typeof components.CrossIcon>
) => {
  return <components.CrossIcon {...props} />;
};
