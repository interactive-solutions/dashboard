import { defineMessages } from 'react-intl';

export const texts = defineMessages({
  pageOf: {
    id: 'Pagination.pageOf',
    description: 'Current page of the total count',
    defaultMessage: 'Sida {pageIndex} av {pageCount}'
  }
});
